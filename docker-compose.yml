version: "2"
services:
  node:
    image: "node:lts"
    user: "node"
    restart: always
    working_dir: /home/node/app
    environment:
      - NODE_ENV=development
    volumes:
      - ./:/home/node/app
    command: >
      sh -c "sudo npm install && sudo npm run app"
