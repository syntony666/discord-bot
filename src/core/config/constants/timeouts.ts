/**
 * Timeout constants for UI interactions (milliseconds)
 */
export const Timeouts = {
  /** Confirmation dialog timeout: 2 minutes */
  CONFIRMATION_MS: 2 * 60 * 1000,

  /** Paginator timeout: 5 minutes */
  PAGINATOR_MS: 5 * 60 * 1000,
} as const;
